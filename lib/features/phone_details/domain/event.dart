class Event {
  final String device;
  final String type;
  final String code;
  final String value;
  final double timestamp;

  Event(this.device, this.type, this.code, this.value, this.timestamp);
}
